# Page templates

Copy the template matching the target layer, rename it with a lowercase kebab-case slug, and replace every `{placeholder}` before validation. Templates are packaging artifacts: they are not indexed, linted as knowledge, or exposed through MCP.

The canonical type-to-layer mapping lives in `SCHEMA.md`.
