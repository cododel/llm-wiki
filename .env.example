# Compose variables only. Generate real files with tools/initialize.ts.
# INSTANCE_DIR must be a protected directory outside this repository.
INSTANCE_DIR=/replace/with/instance-directory
WIKI_HOST=wiki.example.com
AUTH_HOST=auth.example.com
TLS_EMAIL=owner@example.com
